<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">
	<select id="getByEmail" resultType="uservo" parameterType="string">
		<![CDATA[
			select no, name, email, gender
			 from user
			where email=#{email}
		]]>
	</select>
	<insert id="insert" parameterType="uservo">
		<![CDATA[
			insert
			 into user 
			values (null, #{name},#{email},#{password},#{gender},'USER') 
		]]>
	</insert>
	<select id="getByEmailAndPassword" resultType="uservo" parameterType="map">
		<![CDATA[
			select no, name, role
 			 from user 
			where email='dhwnkea@naver.com'
 			 and password = 'tlqkf123';
		]]>
	</select>
</mapper>